/// Test setup for Vitest (jsdom). Runs before each test file.
///
/// In v0.1 we don't yet depend on `@testing-library/svelte`; that
/// lands when real component tests are added (F02 follow-up). Keep
/// this file as the place to register globals / mocks / DOM
/// extensions.
